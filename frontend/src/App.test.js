import { render, screen } from '@testing-library/react';
import App from './App';

test('renders learn react link', () => {
  // This is a basic test that will need to be updated
  // based on your actual application content
  render(<App />);
  // const linkElement = screen.getByText(/EdConnect/i);
  // expect(linkElement).toBeInTheDocument();
});